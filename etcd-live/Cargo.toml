[package]
name = "etcd-live"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
etcd-rs="1.0"
serde = {version = "1", features = ["derive"]}
serde_json = "1"
anyhow = "1"
